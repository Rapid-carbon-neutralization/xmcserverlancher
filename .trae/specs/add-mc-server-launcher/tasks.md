# Tasks

- [x] Task 1: 配置项目依赖与跨平台基础
  - [x] SubTask 1.1: 更新 `pubspec.yaml`，添加依赖：`file_picker`、`path`、`path_provider`、`http`、`provider`（或 `shared_preferences` + 状态管理）、`shared_preferences`/本地 JSON 存储
  - [x] SubTask 1.2: 运行 `flutter pub get` 拉取依赖
  - [x] SubTask 1.3: 确认 Windows / macOS / Linux 平台配置可用；检查 macOS `DebugProfile.entitlements` 与 `Release.entitlements` 包含网络与用户目录访问权限
- [x] Task 2: 建立应用目录结构与主题
  - [x] SubTask 2.1: 在 `lib/` 下规划目录：`models/`、`services/`、`data/`、`screens/`、`widgets/`、`state/`
  - [x] SubTask 2.2: 重写 `lib/main.dart`，配置应用主题（深色/终端风格）、路由与根 Provider
  - [x] SubTask 2.3: 删除默认计数器 `MyHomePage` 代码
- [x] Task 3: 构建服务端核心目录数据层
  - [x] SubTask 3.1: 在 `lib/data/server_cores.dart` 定义核心分类枚举与核心元数据（名称、分类、适用场景描述）
  - [x] SubTask 3.2: 维护 FastMirror 版本与下载地址映射（依据 need.md 表格），覆盖 Vanilla/Paper/Purpur/Folia/Leaves/SpongeVanilla/SpongeForge/Fabric/CatServer/Arclight 等已提供地址的核心
  - [x] SubTask 3.3: 提供核心文件命名规则工具函数（`核心名-版本-链接最后一段值.jar`）
- [x] Task 4: 定义数据模型
  - [x] SubTask 4.1: 定义 `ServerInstance` 模型（id、名称、根目录路径、核心文件路径、启动命令、核心类型、版本、状态、创建时间）
  - [x] SubTask 4.2: 定义 `InstanceStatus` 枚举（starting / running / restarting / stopped）
  - [x] SubTask 4.3: 定义随机命名数据（中文风格名称池）与命名生成函数
- [x] Task 5: 实现实例持久化服务
  - [x] SubTask 5.1: 在 `lib/services/instance_store.dart` 实现本地 JSON 持久化（基于 path_provider 应用目录）
  - [x] SubTask 5.2: 提供加载、保存、新增、删除、重命名接口
- [x] Task 6: 实现下载服务
  - [x] SubTask 6.1: 在 `lib/services/downloader.dart` 实现基于 `http`/`dio` 的文件下载，支持进度回调（已下载字节数、总字节、速度）
  - [x] SubTask 6.2: 下载完成后按命名规则保存到实例目录
- [x] Task 7: 实现服务器进程管理服务
  - [x] SubTask 7.1: 在 `lib/services/server_process.dart` 使用 `dart:io` 的 `Process.start` 启动服务器（按启动命令拆分）
  - [x] SubTask 7.2: 实时读取 stdout/stderr 并通过 Stream 推送日志（带时间戳）
  - [x] SubTask 7.3: 实现 `stop`（向 stdin 写入 `stop\n`）、强制停止（kill 进程）、重启（stop 后等待退出再启动）逻辑
  - [x] SubTask 7.4: 监听进程退出，自动更新状态为「已关闭」
- [x] Task 8: 实现状态管理
  - [x] SubTask 8.1: 创建 `AppState` / `InstanceListNotifier`（provider），管理实例列表与当前选中实例
  - [x] SubTask 8.2: 暴露创建实例（导入/下载/导入核心）、删除、重命名、启动、停止、强制停止、重启方法
- [x] Task 9: 实现引导界面
  - [x] SubTask 9.1: 在 `lib/screens/onboarding_screen.dart` 构建首屏，中央两个卡片按钮：「新建」「导入」及描述
  - [x] SubTask 9.2: 点击「新建」跳转到「新建实例选择」；点击「导入」跳转到「导入实例表单」
- [x] Task 10: 实现「导入实例」表单
  - [x] SubTask 10.1: 在 `lib/screens/import_instance_screen.dart` 提供根目录路径输入 + 浏览按钮（file_picker 选目录）
  - [x] SubTask 10.2: 提供启动命令输入框（预填示例，如 `java -Xmx2G -jar server.jar nogui`）
  - [x] SubTask 10.3: 「创建实例」按钮校验非空后创建实例并返回主页
- [x] Task 11: 实现「新建实例」二级选择
  - [x] SubTask 11.1: 在 `lib/screens/new_instance_screen.dart` 展示「下载」「导入核心」两个子选项及描述
- [x] Task 12: 实现「下载核心」流程
  - [x] SubTask 12.1: 在 `lib/screens/download_core_screen.dart` 构建级联下拉：核心 → 版本（根据核心动态更新）→ 下载源（固定 FastMirror，禁用）
  - [x] SubTask 12.2: 核心选中时在右侧显示该核心的适用场景介绍
  - [x] SubTask 12.3: 「下一步」跳转下载进度页
  - [x] SubTask 12.4: 下载进度页展示进度条、百分比、下载速度
  - [x] SubTask 12.5: 下载完成后切换到启动命令编辑输入框（预填默认命令），点击「完成并创建实例」生成实例并返回主页
- [x] Task 13: 实现「导入核心文件」流程
  - [x] SubTask 13.1: 在 `lib/screens/import_core_screen.dart` 提供核心文件选择（file_picker 选 .jar）与启动命令输入
  - [x] SubTask 13.2: 确认后创建实例并返回主页
- [x] Task 14: 实现实例列表主页
  - [x] SubTask 14.1: 在 `lib/screens/home_screen.dart` 以圆角卡片网格/列表展示实例
  - [x] SubTask 14.2: 每张卡片显示随机名称 + 状态标签（启动中/重启中/已关闭）
  - [x] SubTask 14.3: 点击卡片进入实例详情页
  - [x] SubTask 14.4: 顶部提供「+ 新建实例」入口回到引导/新建流程
- [x] Task 15: 实现实例详情页
  - [x] SubTask 15.1: 在 `lib/screens/instance_detail_screen.dart` 构建顶部导航栏 + 左右两栏
  - [x] SubTask 15.2: 左侧：终端风格日志窗口（实时滚动、时间戳）+ 单行命令输入框（回车发送到 stdin）
  - [x] SubTask 15.3: 右侧：启动 / 重启 / 停止·强制停止 按钮，按状态动态启用/禁用与切换样式
  - [x] SubTask 15.4: 停止按钮点击后立即变为「强制停止」，并保持可点击直至下次启动前恢复
- [x] Task 16: 验证跨平台构建
  - [x] SubTask 16.1: Windows 构建运行验证（`flutter build windows --debug` 成功，产出 `build\windows\x64\runner\Debug\xmcserverlancher.exe`）
  - [x] SubTask 16.2: macOS 构建配置验证（平台目录与 entitlements 完备：网络客户端/服务端 + 用户文件读写权限；需在 macOS 主机实际构建）
  - [x] SubTask 16.3: Linux 构建配置验证（平台目录与 CMakeLists 完备；需在 Linux 主机实际构建）

# Task Dependencies
- Task 2 依赖 Task 1
- Task 3、Task 4 可并行，依赖 Task 2
- Task 5、Task 6、Task 7 依赖 Task 4
- Task 8 依赖 Task 5、Task 7
- Task 9~15 依赖 Task 8（状态）与 Task 3（数据）
- Task 16 依赖 Task 9~15 全部完成
