# Checklist

## 项目基础与依赖
- [x] `pubspec.yaml` 已添加 file_picker、path、path_provider、http、provider、shared_preferences 等依赖
- [x] `flutter pub get` 成功执行，无依赖冲突
- [x] 默认计数器 Demo 代码已移除，`lib/main.dart` 已重写为启动器入口
- [x] macOS `DebugProfile.entitlements` 与 `Release.entitlements` 声明了网络与用户目录访问权限

## 数据层
- [x] 服务端核心目录数据已定义，覆盖原版/插件服/Mod服/混合服四大分类
- [x] 每个核心包含名称、分类、适用场景描述
- [x] FastMirror 版本与下载地址映射已按 need.md 表格填充
- [x] 核心文件命名规则函数实现（`核心名-版本-链接最后一段值.jar`）
- [x] `ServerInstance` 模型字段完整（id、名称、路径、核心路径、启动命令、核心类型、版本、状态、创建时间）
- [x] `InstanceStatus` 枚举包含 starting / running / restarting / stopped
- [x] 随机命名数据池与生成函数已实现

## 服务层
- [x] 实例持久化服务实现（本地 JSON 读写），支持加载/保存/新增/删除/重命名
- [x] 应用重启后实例列表可恢复
- [x] 下载服务实现进度回调（字节数、百分比、速度）
- [x] 下载文件按命名规则保存到实例目录
- [x] 服务器进程管理：使用启动命令正确拆分并 `Process.start`
- [x] stdout/stderr 实时通过 Stream 推送日志（带时间戳）
- [x] `stop` 指令正确写入 stdin
- [x] 强制停止通过 kill 进程实现
- [x] 重启逻辑：stop → 等待退出 → 重新启动
- [x] 进程退出后状态自动更新为「已关闭」

## 状态管理
- [x] 实例列表 Notifier 暴露创建/删除/重命名/启动/停止/强制停止/重启方法
- [x] 状态变更触发 UI 刷新

## 引导界面
- [x] 首屏中央显示「新建」「导入」两个入口及描述文字
- [x] 点击「新建」跳转新建实例选择页
- [x] 点击「导入」跳转导入实例表单页

## 导入实例流程
- [x] 根目录路径输入框 + 浏览按钮（文件选择器选目录）
- [x] 启动命令输入框（预填示例）
- [x] 「创建实例」按钮校验非空后创建实例并返回主页

## 新建实例二级选择
- [x] 展示「下载」「导入核心」两个子选项及描述

## 下载核心流程
- [x] 级联下拉：核心 → 版本（动态更新）→ 下载源（固定 FastMirror，禁用切换）
- [x] 核心选中时右侧显示适用场景介绍
- [x] 「下一步」跳转下载进度页
- [x] 下载进度页展示进度条、百分比、下载速度
- [x] 下载完成后展示启动命令编辑框（预填默认命令）
- [x] 「完成并创建实例」生成实例并返回主页

## 导入核心文件流程
- [x] 核心文件选择（.jar）与启动命令输入
- [x] 确认后创建实例并返回主页

## 实例列表主页
- [x] 实例以圆角卡片展示
- [x] 卡片显示随机名称 + 状态标签（启动中/重启中/已关闭）
- [x] 点击卡片进入实例详情页
- [x] 提供「+ 新建实例」入口

## 实例详情页
- [x] 顶部导航栏 + 左右两栏布局
- [x] 左侧：终端风格日志窗口（实时滚动、时间戳）
- [x] 左侧：单行命令输入框（回车发送到 stdin，无需 `/`）
- [x] 右侧：启动按钮仅在「已关闭」可用
- [x] 右侧：重启按钮仅在「启动中」可用
- [x] 右侧：停止按钮在「启动中」可用，点击后发送 `stop` 并立即变为「强制停止」
- [x] 右侧：强制停止按钮在关闭流程期间及已关闭后均可点击，发送中断信号
- [x] 下次启动前按钮恢复为「停止」

## 跨平台验证
- [x] Windows 平台构建运行正常
- [x] macOS 平台构建运行正常
- [x] Linux 平台构建运行正常
